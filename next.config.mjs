/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'https://refugeehelper.blob.core.windows.net',
      'refugeehelper.blob.core.windows.net'
    ]
  }
};

export default nextConfig;
